#include <stdio.h>
#include <stdlib.h>

int main()
{
    int count=65;
    while(count<=90 && count>=65)
    {
    printf("ASCII value of %c is %d \n",count,count);
    printf("\n");
    count++;
    }

}
